<section class="quote-layout layout">
  <article class="container">
    <h2>{{ title }}</h2>
    <blockquote>
      <cite>
        {{ quote }}
      </cite>
      <br>
      <p>
        {{ author }}
      </p>
    </blockquote>
  </article>
</section>
